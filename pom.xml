<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>org.openengsb.opencit</groupId>
  <artifactId>opencit-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>

  <name>OpenCIT :: Parent</name>

  <description>Continuous Integration Server based on the OpenEngSB</description>

  <properties>
    <openengsb.version>1.0.0-SNAPSHOT</openengsb.version>
    <plugin.assembly.version>2.2-beta-5</plugin.assembly.version>
    <jdk.version>1.6</jdk.version>
    <plugin.pax.version>1.4</plugin.pax.version>
    <plugin.deploy.version>2.5</plugin.deploy.version>
    <maven.plugin.compiler.version>2.3.1</maven.plugin.compiler.version>
    <maven.plugin.enforcer.version>1.0</maven.plugin.enforcer.version>
    <plugin.resources.version>2.4.3</plugin.resources.version>
    <maven.plugin.build.helper.version>1.5</maven.plugin.build.helper.version>
  </properties>

  <packaging>pom</packaging>

  <modules>
    <module>poms</module>
    <module>provision</module>
    <module>assembly</module>
    <module>core</module>
    <module>ui</module>
  </modules>

 <build>
    <finalName>opencit-${project.version}</finalName>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>**/*</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.plugin.compiler.version}</version>
        <configuration>
          <source>${jdk.version}</source>
          <target>${jdk.version}</target>
          <fork>true</fork>
          <meminitial>128m</meminitial>
          <maxmem>512m</maxmem>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.plugin.enforcer.version}</version>
        <executions>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[${mvn.version},)</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
          <execution>
            <id>enforce-java</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>${jdk.version}</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${plugin.deploy.version}</version>
        <configuration>
          <remoteOBR>true</remoteOBR>
        </configuration>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${plugin.resources.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>run</id>
      <activation>
        <activeByDefault>true</activeByDefault>
        <property>
          <name>run</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.ops4j</groupId>
            <artifactId>maven-pax-plugin</artifactId>
            <version>${plugin.pax.version}</version>
            <configuration>
              <runner>1.5.0</runner>
              <framework>felix</framework>
              <noDependencies>true</noDependencies>
              <provision>
                <param>--platform=felix</param>
                <param>--console=false</param>
                <param>--vmOptions=-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005</param>
                <param>scan-composite:file:assembly/target/classes/karaf.composite</param>
                <param>scan-composite:file:assembly/target/classes/settings.debug.composite</param>
                <param>--ee=J2SE-1.6</param>
                <param>--definitionURL=file:assembly/target/classes/felix.xml</param>
                <param>scan-pom:file:assembly/target/provision-provision.xml</param>
              </provision>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>release</id>
      <activation>
        <property>
          <name>release</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.ops4j</groupId>
            <artifactId>maven-pax-plugin</artifactId>
            <version>${plugin.pax.version}</version>
            <configuration>
              <runner>1.5.0</runner>
              <framework>felix</framework>
              <noDependencies>true</noDependencies>
              <provision>
                <param>--platform=felix</param>
                <param>--executor=script</param>
                <param>--workingDirectory=target/all/opencit</param>
                <param>--console=false</param>
                <param>--ee=J2SE-1.6</param>
                <param>--definitionURL=file:assembly/target/classes/felix.xml</param>
                <param>--clean</param>
                <param>scan-pom:file:assembly/target/provision-provision.xml</param>
              </provision>
            </configuration>
          </plugin>
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <version>${plugin.assembly.version}</version>
            <configuration>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>${project.basedir}/assembly/src/main/descriptors/opencit.xml</descriptor>
              </descriptors>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <distributionManagement>
    <snapshotRepository>
      <id>opencit-snapshots</id>
      <name>OpenEngSB Snapshot Repository</name>
      <url>dav:http://maven.openengsb.org/nexus/content/repositories/opencit-snapshots/</url>
    </snapshotRepository>
  </distributionManagement>

  <repositories>
    <repository>
      <id>sonatype.repository</id>
      <url>https://oss.sonatype.org/content/groups/public/</url>
    </repository>
    <repository>
      <id>thirdparty</id>
      <name>OpenEngSB 3rd Party Repository</name>
      <url>http://maven.openengsb.org/nexus/content/repositories/thirdparty</url>      
    </repository>
  </repositories>

</project>

